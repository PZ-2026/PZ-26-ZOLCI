package pl.edu.ur.km131467.trainit.ui.theme

import androidx.compose.material3.Typography
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp

/**
 * Definicja typografii Material 3 dla motywu Jetpack Compose.
 *
 * Plik wygenerowany automatycznie przez Android Studio jako część
 * szablonu projektu Compose. Aktualnie nieużywany — aplikacja
 * korzysta z widoków XML i domyślnych stylów tekstowych.
 *
 * Definiuje jedynie styl `bodyLarge`; pozostałe style (zakomentowane)
 * mogą być odkomentowane i dostosowane w razie potrzeby.
 */
val Typography = Typography(
    bodyLarge = TextStyle(
        fontFamily = FontFamily.Default,
        fontWeight = FontWeight.Normal,
        fontSize = 16.sp,
        lineHeight = 24.sp,
        letterSpacing = 0.5.sp
    )
)
