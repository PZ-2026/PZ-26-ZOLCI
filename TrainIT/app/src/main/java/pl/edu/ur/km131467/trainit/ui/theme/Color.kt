package pl.edu.ur.km131467.trainit.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Definicje kolorów dla motywu Jetpack Compose.
 *
 * Plik wygenerowany automatycznie przez Android Studio jako część
 * szablonu projektu Compose. Aktualnie nieużywany — aplikacja
 * korzysta z widoków XML i kolorów zdefiniowanych w `res/values/colors.xml`.
 */

/** Fioletowy o jasności 80% — kolor podstawowy motywu ciemnego. */
val Purple80 = Color(0xFFD0BCFF)

/** Fioletowo-szary o jasności 80% — kolor drugorzędny motywu ciemnego. */
val PurpleGrey80 = Color(0xFFCCC2DC)

/** Różowy o jasności 80% — kolor trzeciorzędny motywu ciemnego. */
val Pink80 = Color(0xFFEFB8C8)

/** Fioletowy o jasności 40% — kolor podstawowy motywu jasnego. */
val Purple40 = Color(0xFF6650a4)

/** Fioletowo-szary o jasności 40% — kolor drugorzędny motywu jasnego. */
val PurpleGrey40 = Color(0xFF625b71)

/** Różowy o jasności 40% — kolor trzeciorzędny motywu jasnego. */
val Pink40 = Color(0xFF7D5260)
